version: 'alpine'
services:
  fast-api:
    build: ./app
    ports:
      - "80"
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - fast-api
    ports:
      - "8080:8080"
